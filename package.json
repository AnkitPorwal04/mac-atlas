{
  "name": "mac-atlas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "update-registry": "node update-registry.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check update-registry.mjs && node --check public/app.js && node --check public/core.js"
  }
}
